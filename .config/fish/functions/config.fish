function config --wraps='/usr/bin/git --git-dir=/home/still/.dotfiles --work-tree=/home/still' --description 'alias config /usr/bin/git --git-dir=/home/still/.dotfiles --work-tree=/home/still'
  /usr/bin/git --git-dir=/home/still/.dotfiles --work-tree=/home/still $argv
        
end
