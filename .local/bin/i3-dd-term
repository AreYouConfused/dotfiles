#!/bin/bash
if !(xdotool search --class kitty_scratch)
then
	kitty --class kitty_scratch --detach && 
	until i3-msg '[class="kitty_scratch"]' scratchpad show ; do sleep 0.1 ; done
else
	i3-msg '[class="kitty_scratch"]' scratchpad show
fi

